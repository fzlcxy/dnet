# Tasks: use-relative-paths

## Implementation Tasks

### Phase 1: 路径转换功能
- [x] 添加 `_to_relative_path()` 方法将绝对路径转换为相对路径
- [x] 添加 `_to_absolute_path()` 方法将相对路径转换为绝对路径
- [x] 修改 `_save_settings()` 使用相对路径保存
- [x] 修改 `_load_settings()` 将相对路径转换为绝对路径

### Phase 2: 验证
- [x] 语法检查通过

## Deliverables
- [x] 更新后的 gui_main_window.py
